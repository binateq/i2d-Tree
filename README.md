# i2d-Tree

Implementation of [ikd-Tree algorithm](https://arxiv.org/abs/2102.10808) for geo points (k = 2).

This method is used to build dynamically changing _kd_-tree with rebalancing.

Language: Rust.
